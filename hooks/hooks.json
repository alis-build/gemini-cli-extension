{
  "hooks": {
    "BeforeAgent": [
      {
        "matcher": "*",
        "hooks": [
          {
            "name": "alis-route-triggers",
            "type": "command",
            "command": "${extensionPath}${/}hooks${/}route-alis-triggers.sh",
            "timeout": 5000,
            "description": "When the user addresses Alis ('alis, ...'), inject the DBD primer and skill-routing contract."
          }
        ]
      }
    ],
    "BeforeTool": [
      {
        "matcher": ".*(LoadSkill|SpecIt)$",
        "hooks": [
          {
            "name": "alis-inject-skill-session-id",
            "type": "command",
            "command": "${extensionPath}${/}hooks${/}inject-skill-session-id.sh",
            "timeout": 5000,
            "description": "Merge the Gemini session_id into Alis LoadSkill and SpecIt MCP calls."
          }
        ]
      }
    ]
  }
}
